

















// This method fill the arrays with data, here take two parameter, first number of iterations, second takes the data to be filled in */
var myarray = new Array(10).fill({
    name:'khaled',
    familia: 'khatib'
});
console.log(myarray);
